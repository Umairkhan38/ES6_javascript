// JS me execution 2 phase me complete hota hai 
//1 . Memory phase.
// 2. Execution phase.z 
// When you create a memory in Var keyword JS ENGINE 
// Get the memory and set the special keyword that is Undefined. 
// And  That's why we print the variable before execution.
// And Browser Output give in Undefined. 
// And after execution Its print the real value.
// Because when execution started in JS Engine replace the value undefined to actual value.

// That's why we see the actual different.
 
let a;
console.log(a);    // undefine

// 'b' it didnt allocated a memory so thats why it shopws not define 
console.log(b);    //  not define

